package com.example.TutoSpring.util.tools;

import lombok.AccessLevel;
import lombok.NoArgsConstructor;

@NoArgsConstructor(access = AccessLevel.PRIVATE)
public class Validators {

   /* public static void validateParamExist(HelloRequestBody helloRequestBody) {
        var number = helloRequestBody.getNumberInput();
        if (Objects.isNull(number)) {
            throw new BadRequestException("Number should not be null");
        }
    }*/
}
